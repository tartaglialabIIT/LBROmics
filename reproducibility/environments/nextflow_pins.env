# Recovered from Cerase_single_cell/SAMMY-seq/nextflow.log (local copy of a server run log)
# Confirm against the machine that produced manuscript figures before claiming bit-for-bit reproducibility.

NEXTFLOW_VERSION=24.10.4
SAMMYSEQ_GIT_URL=https://github.com/nf-core/sammyseq
# Observed launch of -r dev (test run logged 2025-02-03):
SAMMYSEQ_GIT_COMMIT=fa6f6ffeb3
SAMMYSEQ_GIT_REF=dev
# Note in same log: remote had a newer tip [638a583cc3] — local asset was outdated at launch time.
# compartments_subworkflow: USED in cmd_to_run.sh; commit NOT present in this nextflow.log excerpt — still TODO on the analysis server.
SAMMYSEQ_COMPARTMENTS_REF=compartments_subworkflow
SAMMYSEQ_COMPARTMENTS_COMMIT=TODO_CONFIRM_ON_SERVER

CHIPSEQ_GIT_COMMIT=TODO_NOT_FOUND_LOCALLY
CHIPSEQ_RELEASE=TODO_NOT_FOUND_LOCALLY

EXECUTOR_PROFILE=docker
CONTAINER_ENGINE=docker
CONTAINER_DIGESTS=TODO
CUSTOM_CONFIG_PATH=none_local_copy

# Machine hints from log / paths:
#   host user: jonny ; paths /mnt/storage/jonni and /mnt/large/jfiorentino
#   proxy.crg.es seen in log (CRG/cluster context for at least one run)
