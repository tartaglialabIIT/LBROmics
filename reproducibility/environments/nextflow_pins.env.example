# Nextflow / nf-core pin template — FILL IN; do not invent values
#
# Preprint Methods cite:
#   https://nf-co.re/sammyseq/dev/
#   https://github.com/nf-core/sammyseq/tree/compartments_subworkflow
#   nf-core/chipseq (version not stated)
#
# Required author fields:

NEXTFLOW_VERSION=TODO
SAMMYSEQ_GIT_URL=https://github.com/nf-core/sammyseq
SAMMYSEQ_GIT_COMMIT=TODO
SAMMYSEQ_GIT_REF=TODO          # tag or branch actually used
CHIPSEQ_GIT_COMMIT=TODO
CHIPSEQ_RELEASE=TODO
EXECUTOR_PROFILE=TODO          # e.g. singularity, docker, conda, slurm
CONTAINER_ENGINE=TODO
CONTAINER_DIGESTS=TODO
CUSTOM_CONFIG_PATH=TODO

# Example launch placeholders (DO NOT run until pins are filled):
# nextflow run nf-core/sammyseq -r $SAMMYSEQ_GIT_COMMIT \
#   -profile $EXECUTOR_PROFILE \
#   --input samplesheet.csv \
#   --genome mm10 \
#   --differential_solubility \
#   --outdir results/sammyseq
