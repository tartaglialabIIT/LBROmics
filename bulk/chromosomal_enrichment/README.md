# Chromosomal enrichment

Fisher exact tests for chromosomal enrichment of DEGs (manuscript Methods: scipy / statsmodels).

Place count tables under `inputs/` (see notebook header). Outputs are written to `outputs/`.
